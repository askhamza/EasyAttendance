@page
@model Assiduite.Pages.Matieres.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Gestion de filière</h1>

<p>
    <a asp-page="Create">Ajouter une nouvelle matière</a>
</p>

<div class="row">
    <div class="col-md-4">
        <form method="post" asp-page-handler="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Matiere.Nom_Mat" class="control-label"></label>
                <input asp-for="Matiere.Nom_Mat" class="form-control" />
                <span asp-validation-for="Matiere.Nom_Mat" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Ajouter" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<table class="table" id="MatiereTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Matieres[0].Nom_Mat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Matieres)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nom_Mat)
                </td>
                <td>
                    <a data-toggle="modal" data-target="#edit_@item.Id_Mat" asp-route-id="@item.Id_Mat">Informations</a>
                    <a data-toggle="modal" data-target="#del_@item.Id_Mat" asp-route-id="@item.Id_Mat">Supprimer</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@foreach (var item in Model.Matieres)
{
    <!-- Modal Edit -->
    <div class="modal fade" id="edit_@item.Id_Mat" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content style-modal">
                <div class="modal-header">
                    <img src="~/EasyAttendance.png" style=" margin-left: auto; margin-right: auto; width:2rem ; height:auto" />
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <h5 style="text-align:center; font-weight: bold"> Easy Attendance </h5>
                <div class="modal-body">
                    <form asp-page-handler="Edit" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="Matiere.Id_Mat" value="@item.Id_Mat" />
                        <div class="row" style="display: flex ; justify-content: center;">
                            <div style="width: 13rem;">
                                <div class="form-group" style="text-align:center">
                                    <label>Nom de la matière</label>
                                    <input style="text-align:center ; color:#356c96;" class="form-control font-weight-bold" value="@item.Nom_Mat" disabled />
                                    <span class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label asp-for="Matiere.Nom_Mat">Nom de la filière</label>
                                    <input asp-for="Matiere.Nom_Mat" class="form-control font-inputs-modal" value="@item.Nom_Mat" />
                                    <span asp-validation-for="Matiere.Nom_Mat" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <div class="card-footer" style="text-align:center">
                            <button type="submit" class="btn btn-fill btn-primary">Enregistrer les modifications</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!--Modal Delete-->
    <div class="modal fade" id="del_@item.Id_Mat" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content style-modal" style="top: 100px ; border-radius: 2rem;">
                <div class="modal-header">
                    <img src="~/EasyAttendance.png" style=" margin-left: auto; margin-right: auto; width:2rem ; height:auto" />
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <h5 class="font-modal" style="text-align:center;"> Easy Attendance </h5>
                <div class="modal-body">
                    <p class="font-inputs-modal" style="font-weight:bold ; text-align:center">
                        Est-ce que vous voullez vraiment supprimer la matière <span class="font-del">@item.Nom_Mat</span>
                    </p>
                    <form asp-page-handler="Delete" method="post">
                        <div class="row" style="align-content:center">
                            <div class="col-md-3"></div>
                            <div class="col-md-6">
                                <div class="form-group" style="text-align:center">
                                    <input type="hidden" name="id" value="@item.Id_Mat" />
                                    <input type="submit" value="Delete @item.Id_Mat" class="btn btn-danger" />
                                </div>
                            </div>
                            <div class="col-md-3"></div>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
}

@section Scripts {

    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        $(document).ready(function () {
            var table = $('#MatiereTable').DataTable({
                "pagingType": "simple",
                "language": {
                    "lengthMenu": "Affichage _MENU_ matières par page",
                    "info": "Page: _PAGE_ sur _PAGES_",
                },
                "paginate": {
                    "first": "Première",
                    "last": "Dernière",
                    "previous": "Precedent",
                    "next": "Suivant",
                }
            });
            $(table.table().container()).removeClass('form-inline');
        });
    </script>

}
